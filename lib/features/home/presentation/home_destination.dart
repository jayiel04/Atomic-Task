enum HomeDestination {
  tasks('Tareas'),
  focus('Concentración'),
  alarm('Alarma'),
  settings('Ajustes'),
  statistics('Estadísticas');

  const HomeDestination(this.title);

  final String title;
}
